# Dalton Solar — pinned runtime dependencies.
#
# Pinned to the versions the full test suite passes against. Do NOT upgrade
# casually: reportlab, pdfplumber and Pillow interact through PDF rendering,
# and PyJWT's API changed across majors.
#
# Verify against your own environment before deploying:
#     pip freeze | findstr /I "flask pyjwt reportlab pdfplumber pytesseract pillow"

Flask==3.1.3
Werkzeug==3.1.8
PyJWT==2.7.0
reportlab==4.4.10
pdfplumber==0.11.4
pytesseract==0.3.13
Pillow==10.4.0
requests==2.34.2

# WSGI server for hosted deployment. Not used by local `python app.py`.
gunicorn==23.0.0

pypdf==5.9.0
